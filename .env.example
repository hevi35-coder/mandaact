# Supabase
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Cloud Platform (for Vision API - Edge Function용)
# ⚠️ 아래 키들은 Supabase Edge Function에서만 사용됩니다
# Supabase Secrets에 설정이 필요합니다:
# $ supabase secrets set GCP_PROJECT_ID=your_project_id
# $ supabase secrets set GCP_CLIENT_EMAIL=your_email
# $ supabase secrets set GCP_PRIVATE_KEY=your_key
GCP_PROJECT_ID=your_gcp_project_id
GCP_CLIENT_EMAIL=your_service_account_email
GCP_PRIVATE_KEY=your_service_account_private_key

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ⚠️ Perplexity API (AI Coaching - Edge Function 전용)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 중요: 이 키는 .env.local 파일에 저장하지 마세요!
#
# 이유:
# 1. 보안: 프론트엔드에서 API 키가 노출되면 브라우저에서 탈취 가능
# 2. 실행 환경: Edge Function은 서버에서 실행되어 로컬 파일 접근 불가
# 3. 비용 관리: 서버에서만 API를 호출해야 사용량 제어 가능
#
# 설정 방법:
# Supabase Secrets에만 설정하면 됩니다:
#
#   $ supabase secrets set PERPLEXITY_API_KEY=pplx-xxxxx
#
# 로컬 개발 시에도 Edge Function을 통해 API가 호출되므로
# 프론트엔드 환경변수(.env.local)가 아닌 Supabase Secrets만 필요합니다.
#
# 자세한 내용: docs/PHASE_4B_SETUP.md 참조
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
