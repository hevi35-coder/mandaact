# MandaAct AdMob 수익화 전략

**최종 업데이트**: 2025-12-06
**버전**: 1.0
**상태**: 계획 수립 완료

---

## 1. 리서치 요약

### 1.1 AdMob 성공 사례 분석

#### 글로벌 성공 사례
| 앱 | 전략 | 성과 |
|---|------|------|
| **Duolingo** | 하트 시스템 + 보상형 광고 | 유저가 광고 시청으로 하트 복구, 높은 전환율 |
| **Balloon Island** | 보상형 비디오 중심 | 일일 $2,000 수익, 우수한 UX 유지 |
| **Evernote** | Freemium + 기능 제한 | 2.25억 유저, 프리미엄 전환 성공 |
| **Canva** | 무료 핵심 기능 + 프리미엄 에셋 | 6,000만 MAU (2024) |
| **Trello** | 유저 수 제한 없음 + 통합 제한 | 팀 성장 시 자연스러운 업그레이드 유도 |

#### 핵심 인사이트
1. **하이브리드 모델 채택률 50%+ 증가** (광고 + 인앱구매 공존)
2. **보상형 광고가 전면 광고보다 2배 효과적**
3. **53% 유저가 보상 때문에 더 오래 사용**
4. **50% 유저가 보상형 광고 제거 시 만족도 하락** (Google 연구)

### 1.2 한국 유저 특성

#### 구독 지출 현황
- 평균 **3.4개 서비스 구독**, 월 **약 4만원** 지출 (방고 2024)
- OTT(84%), 음원(49%), 쇼핑(46%) 순으로 구독

#### 광고 피로도 요인 (한국광고홍보학회 연구)
- **프라이버시 염려** → 광고 회피 증가
- **광고 피로도** → 침입성 지각 → 회피
- **지각된 유용성** → 광고 회피 감소

#### 한국 시장 가격 벤치마크
| 서비스 | 월 구독료 |
|--------|----------|
| 네이버 플러스 | 4,900원 |
| 유튜브 프리미엄 | 14,900원 |
| 쿠팡 로켓와우 | 7,890원 |
| Notion (개인) | 무료~$10 |

### 1.3 생산성 앱 수익화 모범 사례

#### Duolingo 모델 (가장 참고할 만한 사례)
```
무료 유저:
- 하트 5개 제한 (틀리면 소모)
- 하트 복구: 광고 시청 OR 4시간 대기 OR 연습
- XP 부스트: 광고 시청으로 2배 획득

프리미엄 유저:
- 무제한 하트
- 광고 제거
- 오프라인 학습
- 진행 퀴즈
```

---

## 2. MandaAct 광고 배치 전략

### 2.1 광고 유형별 배치 계획

#### A. 배너 광고 (Banner Ads)
| 화면 | 위치 | 조건 | 빈도 |
|------|------|------|------|
| HomeScreen | 하단 고정 | 무료 유저만 | 상시 |
| TodayScreen | 하단 고정 | 무료 유저만 | 상시 |
| MandalartListScreen | 하단 고정 | 무료 유저만 | 상시 |

**설계 원칙**:
- 콘텐츠 영역과 겹치지 않도록 `paddingBottom` 확보
- 배너 높이: 50dp (표준)
- 스크롤 시에도 고정 위치 유지

#### B. 보상형 광고 (Rewarded Ads) - **핵심 수익원**

| 기능 | 보상 | 쿨다운 | 설명 |
|------|------|--------|------|
| **AI 리포트 생성** | 즉시 생성 | 7일 (무료 1회) | 광고 없이 주 1회 무료, 추가는 광고 시청 |
| **XP 2배 부스트** | 30분간 XP 2배 | 24시간 | 하루 1회 광고로 부스트 획득 |
| **스트릭 프리즈** | 1일 프리즈 | 7일 | 스트릭 끊기기 전 광고로 복구 |
| **추가 체크 기회** | 어제 체크 허용 | 24시간 | 깜빡한 체크 복구 |

**설계 원칙**:
- **Opt-in 방식**: 유저가 선택해서 시청
- **명확한 보상 표시**: "광고 보고 XP 2배 받기"
- **쿨다운 명시**: "다음 사용 가능: 23시간 후"

#### C. 전면 광고 (Interstitial Ads)

| 트리거 | 빈도 제한 | 조건 |
|--------|----------|------|
| 만다라트 생성 완료 | 세션당 1회 | 무료 유저만 |
| 주간 리포트 생성 후 | 주 1회 | 무료 유저만 |

**설계 원칙**:
- **자연스러운 전환점**에만 표시
- **세션당 최대 2회** 제한
- 앱 시작 시 절대 표시하지 않음

### 2.2 화면별 광고 배치 맵

```
┌─────────────────────────────────────────────────────────┐
│  HomeScreen                                             │
│  ┌─────────────────────────────────────────────────────┐│
│  │ 대시보드 콘텐츠                                      ││
│  │ - XP/레벨 카드                                      ││
│  │ - 스트릭 정보                                       ││
│  │ - 오늘의 진행상황                                    ││
│  │                                                     ││
│  │ [🎬 광고 보고 XP 2배 받기] ← 보상형 광고 버튼       ││
│  └─────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────┐│
│  │ 📢 배너 광고 (50dp)                                 ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  TodayScreen                                            │
│  ┌─────────────────────────────────────────────────────┐│
│  │ 오늘의 실천 리스트                                   ││
│  │ □ 액션 1                                            ││
│  │ ☑ 액션 2                                            ││
│  │ □ 액션 3                                            ││
│  │ ...                                                 ││
│  │                                                     ││
│  │ [⏰ 어제 체크 복구하기] ← 보상형 광고 버튼          ││
│  └─────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────┐│
│  │ 📢 배너 광고 (50dp)                                 ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ReportsScreen                                          │
│  ┌─────────────────────────────────────────────────────┐│
│  │ 이번 주 리포트                                       ││
│  │                                                     ││
│  │ 무료 생성: 주 1회                                    ││
│  │ ┌───────────────────────────────────────────────┐  ││
│  │ │ [📊 리포트 생성] 또는 [🎬 광고 보고 지금 생성] │  ││
│  │ └───────────────────────────────────────────────┘  ││
│  │                                                     ││
│  │ 다음 무료 생성: 5일 후                              ││
│  └─────────────────────────────────────────────────────┘│
│  (배너 광고 없음 - 콘텐츠 집중)                          │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 광고 피로도 감소 전략

### 3.1 Frequency Capping (노출 빈도 제한)

| 광고 유형 | 일일 제한 | 주간 제한 | 월간 제한 |
|----------|----------|----------|----------|
| 배너 | 상시 (무료 유저) | - | - |
| 전면 | 2회 | 5회 | - |
| 보상형 | 무제한 (Opt-in) | - | - |

### 3.2 Smart Cooldown 시스템

```typescript
interface AdCooldown {
  type: 'banner' | 'interstitial' | 'rewarded';
  lastShown: Date;
  cooldownMinutes: number;
  dailyCount: number;
  maxDailyCount: number;
}

// 예시: 전면 광고 쿨다운
const interstitialCooldown: AdCooldown = {
  type: 'interstitial',
  lastShown: new Date(),
  cooldownMinutes: 180, // 3시간
  dailyCount: 0,
  maxDailyCount: 2
};
```

### 3.3 신규 유저 보호 기간

| 기간 | 광고 정책 |
|------|----------|
| **Day 1-3** | 광고 완전 미노출 (온보딩 집중) |
| **Day 4-7** | 배너만 노출 (전면/보상형 비활성) |
| **Day 8+** | 전체 광고 활성화 |

**근거**:
- 39% 유저가 과도한 알림/광고로 앱 삭제 (2021 연구)
- Day 1 리텐션이 가장 중요, 첫인상 보호 필요

### 3.4 사용 패턴 기반 광고 조절

```typescript
// 활성 유저에게는 광고 줄이기
function getAdFrequency(user: User): 'low' | 'medium' | 'high' {
  const weeklyChecks = user.weeklyCheckCount;
  const streak = user.currentStreak;

  if (weeklyChecks >= 30 || streak >= 30) {
    return 'low';  // 충성 유저: 광고 최소화
  } else if (weeklyChecks >= 15 || streak >= 7) {
    return 'medium';
  }
  return 'high';  // 신규/비활성 유저: 일반 노출
}
```

### 3.5 광고 피로도 감지 및 대응

| 신호 | 대응 |
|------|------|
| 광고 닫기 속도 < 1초 | 24시간 전면 광고 중단 |
| 연속 3회 광고 스킵 | 48시간 전면 광고 중단 |
| 앱 이탈 후 광고 | 다음 세션 광고 미노출 |

---

## 4. 보상형 광고 상세 설계

### 4.1 AI 리포트 생성 시스템 (개선)

#### 현재 → 변경
| 항목 | 현재 | 변경 후 |
|------|------|---------|
| 생성 조건 | 주 1회 무제한 | 주 1회 무료 + 광고로 추가 생성 |
| 쿨다운 | 7일 | 7일 (무료) / 즉시 (광고) |
| 추가 생성 | 불가 | 광고 시청 시 즉시 가능 |

#### 플로우
```
유저가 "리포트 생성" 클릭
    ↓
무료 사용 가능? ─Yes→ 바로 생성
    │
   No (이번 주 사용함)
    ↓
┌─────────────────────────────────────┐
│ "이번 주 무료 생성을 사용했습니다"   │
│                                     │
│ [🎬 광고 보고 지금 생성하기]        │
│                                     │
│ 다음 무료 생성까지: 5일 12시간       │
└─────────────────────────────────────┘
```

### 4.2 XP 2배 부스트 시스템 (신규)

```typescript
interface XPBoost {
  isActive: boolean;
  activatedAt: Date | null;
  durationMinutes: 30;
  multiplier: 2;
  nextAvailable: Date;
  source: 'rewarded_ad' | 'premium';
}

// 사용 예시
function activateXPBoost(userId: string): XPBoost {
  return {
    isActive: true,
    activatedAt: new Date(),
    durationMinutes: 30,
    multiplier: 2,
    nextAvailable: addHours(new Date(), 24),
    source: 'rewarded_ad'
  };
}
```

#### UI 표시
```
┌─────────────────────────────────────┐
│ 🔥 XP 2배 부스트 활성화!            │
│ ████████████░░░░░░░░ 23분 남음       │
│                                     │
│ 지금까지 +240 XP 추가 획득!          │
└─────────────────────────────────────┘
```

### 4.3 스트릭 프리즈 시스템 (개선)

#### 현재 → 변경
| 항목 | 현재 | 변경 후 |
|------|------|---------|
| 프리즈 획득 | 제한적 | 광고로 주 1회 획득 가능 |
| 사용 조건 | 사전 보유 | 스트릭 끊기기 직전 사용 가능 |

#### 위기 알림
```
┌─────────────────────────────────────┐
│ ⚠️ 스트릭 위험!                      │
│                                     │
│ 오늘 체크하지 않으면 15일 스트릭이   │
│ 끊어집니다.                          │
│                                     │
│ [🎬 광고 보고 오늘 하루 프리즈]      │
│ [✓ 지금 체크하러 가기]              │
└─────────────────────────────────────┘
```

### 4.4 어제 체크 복구 (신규)

```
어제 체크 깜빡했어요?
광고를 시청하면 어제 날짜로 체크할 수 있습니다.

[🎬 광고 보고 어제 체크 복구하기]

* 하루에 1회만 사용 가능합니다
```

---

## 5. 무료/유료 모델 설계

### 5.1 기능 비교

| 기능 | 무료 | Premium |
|------|------|---------|
| **만다라트 관리** | 3개 | 무제한 |
| **일일 체크** | 무제한 | 무제한 |
| **게임화 (XP/배지)** | 전체 | 전체 |
| **AI 리포트** | 주 1회 (or 광고) | 무제한 |
| **목표 진단** | 월 1회 (or 광고) | 무제한 |
| **광고** | 배너 + 전면 | 없음 |
| **XP 부스트** | 광고 시청 | 매일 1회 무료 |
| **스트릭 프리즈** | 광고 시청 | 월 4회 무료 |
| **어제 체크 복구** | 광고 시청 | 무제한 |
| **통계 상세** | 기본 | 고급 분석 |
| **테마/커스터마이징** | 기본 | 프리미엄 테마 |
| **우선 지원** | 일반 | 24시간 내 응답 |

### 5.2 가격 전략

#### 한국 시장 가격 벤치마크
- 네이버 플러스: 4,900원/월
- 유튜브 프리미엄: 14,900원/월
- 쿠팡 와우: 7,890원/월
- Notion Pro: ~$10 (약 13,000원)/월

#### MandaAct Premium 가격안

| 옵션 | 가격 | 월 환산 | 할인율 |
|------|------|---------|--------|
| **월간 구독** | 4,900원/월 | 4,900원 | - |
| **연간 구독** | 39,000원/년 | 3,250원 | 33% 할인 |
| **평생 이용권** | 99,000원 | - | 일회성 |

**가격 선정 근거**:
1. 한국 평균 구독 지출 4만원/월 대비 합리적
2. 네이버 플러스와 동일선 (친숙한 가격대)
3. 생산성 앱 중 저가 포지셔닝 (진입 장벽 낮춤)

### 5.3 Premium 전환 유도 전략

#### A. Soft Paywall
```
무료 리포트 사용 완료!

Premium으로 업그레이드하면:
✓ 언제든지 AI 리포트 생성
✓ 광고 없는 깔끔한 경험
✓ XP 부스트 매일 무료

[Premium 시작하기 - 첫 달 50% 할인]
[광고 보고 이번만 생성하기]
```

#### B. 레벨업 시 노출
```
🎉 레벨 10 달성!

Premium 유저로서 받을 수 있는 혜택:
- 레벨 10 전용 배지
- 프리미엄 테마 해제
- 광고 없는 집중 환경

[7일 무료 체험 시작]
```

#### C. 스트릭 위기 시 노출
```
15일 스트릭을 지키세요!

Premium이면 월 4회 자동 프리즈로
스트릭을 안전하게 보호합니다.

[Premium으로 스트릭 보호하기]
```

### 5.4 프리미엄 전환 퍼널

```
DAY 1-7: 신규 유저 보호
    ↓
DAY 8-14: 배너 광고 시작 (전환점 1)
    ↓
DAY 15-30: 보상형 광고 소개 (전환점 2)
    ↓
LEVEL 5: Premium 프로모션 시작 (전환점 3)
    ↓
STREAK 7+: 프리즈 혜택 강조 (전환점 4)
    ↓
리포트 제한 도달: Soft Paywall (전환점 5)
```

---

## 6. 기술 구현 계획

### 6.1 데이터베이스 스키마

```sql
-- 사용자 구독 상태
CREATE TABLE user_subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users NOT NULL,
  plan_type TEXT NOT NULL DEFAULT 'free', -- 'free', 'premium', 'lifetime'
  status TEXT NOT NULL DEFAULT 'active', -- 'active', 'canceled', 'expired'
  started_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ,
  platform TEXT, -- 'ios', 'android', 'web'
  store_product_id TEXT,
  store_transaction_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 광고 시청 기록
CREATE TABLE ad_watch_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users NOT NULL,
  ad_type TEXT NOT NULL, -- 'banner', 'interstitial', 'rewarded'
  reward_type TEXT, -- 'xp_boost', 'report', 'streak_freeze', 'yesterday_check'
  reward_value JSONB,
  watched_at TIMESTAMPTZ DEFAULT NOW()
);

-- 보상형 광고 쿨다운
CREATE TABLE rewarded_ad_cooldowns (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users NOT NULL,
  reward_type TEXT NOT NULL,
  last_used_at TIMESTAMPTZ NOT NULL,
  next_available_at TIMESTAMPTZ NOT NULL,
  UNIQUE(user_id, reward_type)
);

-- XP 부스트 상태
CREATE TABLE xp_boosts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users NOT NULL,
  source TEXT NOT NULL, -- 'rewarded_ad', 'premium', 'event'
  multiplier NUMERIC DEFAULT 2,
  activated_at TIMESTAMPTZ NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  is_active BOOLEAN DEFAULT true
);
```

### 6.2 필요한 패키지

```json
{
  "dependencies": {
    "react-native-google-mobile-ads": "^14.0.0",
    "expo-tracking-transparency": "~4.0.0",
    "react-native-purchases": "^8.0.0"
  }
}
```

### 6.3 구현 우선순위

| Phase | 기능 | 예상 기간 |
|-------|------|----------|
| **1** | AdMob SDK 통합 + 배너 광고 | 1일 |
| **2** | 보상형 광고 (XP 부스트) | 1일 |
| **3** | 리포트 생성 광고 연동 | 0.5일 |
| **4** | 전면 광고 + Frequency Capping | 0.5일 |
| **5** | 신규 유저 보호 기간 | 0.5일 |
| **6** | Premium 구독 (IAP) | 2일 |
| **7** | Premium 기능 분기 처리 | 1일 |
| **8** | 광고 피로도 추적/최적화 | 1일 |

**총 예상 기간**: 7-8일

---

## 7. 성공 지표 (KPIs)

### 7.1 광고 수익 지표

| 지표 | 목표 (3개월) |
|------|-------------|
| eCPM (배너) | $1-2 |
| eCPM (전면) | $5-10 |
| eCPM (보상형) | $10-20 |
| 광고 ARPDAU | $0.02-0.05 |
| 보상형 광고 시청률 | >30% |

### 7.2 구독 전환 지표

| 지표 | 목표 (6개월) |
|------|-------------|
| Free → Premium 전환율 | 3-5% |
| 구독 유지율 (월간) | >85% |
| 연간 구독 비율 | >40% |

### 7.3 사용자 경험 지표

| 지표 | 기준 |
|------|------|
| 광고로 인한 이탈률 | <5% |
| 광고 관련 1점 리뷰 | <10% |
| D7 리텐션 (광고 후) | 기존 대비 -5% 이내 |

---

## 8. 정책 준수

### 8.1 앱스토어 정책

| 정책 | 대응 |
|------|------|
| **Apple ATT** | 앱 시작 시 추적 동의 요청 (iOS 14.5+) |
| **GDPR** | EU 유저에게 동의 배너 표시 |
| **COPPA** | 연령 확인 없음 (전연령 앱) |
| **Google Play 광고 정책** | 광고 라벨 명시, 클릭 유도 금지 |

### 8.2 GDPR 동의 플로우

```
앱 첫 실행 시 (EU 유저):

┌─────────────────────────────────────┐
│ 개인 맞춤 광고 동의                  │
│                                     │
│ MandaAct는 더 나은 광고 경험을 위해  │
│ 익명화된 사용 데이터를 수집합니다.   │
│                                     │
│ [동의하고 계속하기]                  │
│ [거부하고 계속하기]                  │
│                                     │
│ 언제든 설정에서 변경할 수 있습니다.  │
└─────────────────────────────────────┘
```

---

## 9. 리스크 및 대응

| 리스크 | 확률 | 영향 | 대응 |
|--------|------|------|------|
| 광고로 인한 리텐션 하락 | 중 | 높음 | 신규 유저 보호 + 피로도 모니터링 |
| 낮은 eCPM | 중 | 중 | AdMob 미디에이션 + A/B 테스트 |
| 앱스토어 리젝 | 낮 | 높음 | 정책 사전 검토 + 테스트 광고 사용 |
| Premium 전환율 저조 | 중 | 중 | 가치 제안 강화 + 할인 프로모션 |
| 결제 처리 이슈 | 낮 | 중 | RevenueCat 사용 검토 |

---

## 10. 참고 자료

### 리서치 출처
- [Google AdMob 성공 사례](https://www.google.com/intl/ko_kr/admob/success.html)
- [5가지 앱 수익화 전략 - AdMob](https://admob.google.com/intl/ko/home/resources/5-app-monetization-strategies-to-grow-and-monetize-your-app/)
- [보상형 광고 베스트 프랙티스 - Adjust](https://www.adjust.com/blog/understanding-rewarded-video-ads/)
- [Freemium 모델 성공 사례](https://moldstud.com/articles/p-freemium-model-success-stories-apps-that-got-it-right-and-how-they-did-it)
- [한국광고홍보학회 - 광고 회피 연구](https://www.dbpia.co.kr/Journal/articleDetail?nodeId=NODE08007248)
- [잘 나가는 구독 서비스 가격 책정 전략](https://yozm.wishket.com/magazine/detail/1234/)
- [Frequency Capping 가이드](https://yepads.com/frequency-capping-explained-stop-ad-fatigue/)
- [모바일 앱 리텐션 전략](https://adjoe.io/blog/mobile-app-retention-strategies/)

---

## 11. 기존 시스템 영향도 분석

> **중요**: 이 섹션은 AdMob 연동 시 기존 시스템과의 통합 포인트를 정의합니다.
> 각 작업 수행 전 반드시 해당 영향도를 검토하세요.

### 11.1 XP 시스템 연동

#### 영향받는 파일
| 파일 | 변경 내용 | 우선순위 |
|------|----------|----------|
| `apps/web/src/lib/xpMultipliers.ts` | XP 부스트 소스에 `'rewarded_ad'` 추가 | 🔴 |
| `apps/web/src/lib/stats.ts` | XP 계산 시 부스트 상태 확인 로직 | 🔴 |
| `apps/mobile/src/hooks/useStats.ts` | 동일하게 부스트 로직 추가 | 🔴 |
| `packages/shared/src/xpUtils.ts` | 공통 XP 계산 로직 확장 | 🟡 |

#### 기존 XP Multiplier 타입 확장
```typescript
// 현재 xpMultipliers.ts
export type MultiplierType = 'weekend' | 'comeback' | 'level_milestone' | 'perfect_week'

// 변경 후
export type MultiplierType = 'weekend' | 'comeback' | 'level_milestone' | 'perfect_week' | 'ad_boost'

// 새로운 인터페이스 추가
export interface AdXPBoost {
  isActive: boolean;
  activatedAt: Date | null;
  expiresAt: Date | null;
  multiplier: number; // 2
  source: 'rewarded_ad' | 'premium';
}
```

#### 변경 체크리스트
- [ ] `xpMultipliers.ts`에 `ad_boost` 타입 추가
- [ ] `getActiveMultipliers()` 함수에 XP 부스트 체크 로직 추가
- [ ] `calculateTotalMultiplier()` 함수에 부스트 배율 적용
- [ ] `xp_boosts` 테이블과 연동
- [ ] Mobile 앱 동기화

---

### 11.2 배지 시스템 연동

#### 영향받는 파일
| 파일 | 변경 내용 | 우선순위 |
|------|----------|----------|
| `apps/web/src/lib/badgeEvaluator.ts` | Premium 전용 배지 잠금 로직 | 🟡 |
| `supabase/migrations/` | 새로운 배지 추가 마이그레이션 | 🟡 |

#### Premium 전용 배지 (선택적)
| 배지 | 조건 | 설명 |
|------|------|------|
| `premium_supporter` | Premium 구독 | "후원자" 배지 |
| `ad_free_master` | Premium 30일 유지 | "광고 없는 집중" 배지 |
| `streak_guardian` | Premium 프리즈 3회 사용 | "스트릭 수호자" 배지 |

#### 변경 체크리스트
- [ ] Premium 전용 배지 스키마 설계 (선택적)
- [ ] `badgeEvaluator.ts`에 Premium 상태 체크 추가
- [ ] 배지 UI에서 Premium 배지 잠금 표시

---

### 11.3 스트릭 시스템 연동

#### 영향받는 파일
| 파일 | 변경 내용 | 우선순위 |
|------|----------|----------|
| `apps/web/src/lib/stats.ts` | 스트릭 프리즈 로직 확장 | 🔴 |
| `apps/mobile/src/hooks/useStats.ts` | 동일하게 프리즈 로직 확장 | 🔴 |
| `supabase/functions/streak-warning/` | 광고 시청 옵션 안내 추가 | 🟡 |

#### 현재 스트릭 프리즈 → 변경
```typescript
// 현재: 사전 보유 프리즈만 사용
interface StreakFreeze {
  available: number;
  usedToday: boolean;
}

// 변경 후: 광고/Premium 통해 획득 가능
interface StreakFreeze {
  available: number;
  usedToday: boolean;
  canWatchAd: boolean;          // 광고로 프리즈 획득 가능 여부
  adCooldownEndsAt: Date | null; // 다음 광고 가능 시간
  premiumFreezeRemaining: number; // Premium 월 무료 프리즈
}
```

#### 변경 체크리스트
- [ ] 스트릭 프리즈 획득 경로 추가 (광고, Premium)
- [ ] `streak-warning` Edge Function에 광고 옵션 안내 추가
- [ ] 프리즈 사용 이력 테이블 확장 (`rewarded_ad_cooldowns`)
- [ ] 스트릭 위기 푸시 알림에 광고 CTA 포함

---

### 11.4 리포트 시스템 연동

#### 영향받는 파일
| 파일 | 변경 내용 | 우선순위 |
|------|----------|----------|
| `supabase/functions/generate-report/` | 무료/광고/Premium 분기 | 🔴 |
| `apps/web/src/pages/ReportsPage.tsx` | 리포트 생성 버튼 UI 변경 | 🔴 |
| `apps/mobile/src/screens/ReportsScreen.tsx` | 동일하게 UI 변경 | 🔴 |

#### 현재 리포트 생성 → 변경
```typescript
// 현재: 단순 생성
async function generateReport(type: ReportType): Promise<Report>

// 변경 후: 권한 체크 포함
async function generateReport(
  type: ReportType,
  accessMethod: 'free' | 'rewarded_ad' | 'premium'
): Promise<Report | { requiresAd: true; cooldownEndsAt: Date }>
```

#### 변경 체크리스트
- [ ] `generate-report` Edge Function에 사용 횟수 체크 추가
- [ ] 무료 사용 횟수 테이블 (`report_usage`) 생성
- [ ] 광고 시청 후 즉시 생성 로직 추가
- [ ] Premium 유저 무제한 생성 로직 추가
- [ ] ReportsPage/ReportsScreen UI에 "광고 보고 생성" 버튼 추가

---

### 11.5 푸시 알림 연동

#### 영향받는 파일
| 파일 | 변경 내용 | 우선순위 |
|------|----------|----------|
| `supabase/functions/streak-warning/` | 광고 CTA 포함 알림 | 🟡 |
| `supabase/functions/comeback-notification/` | XP 부스트 안내 포함 | 🟡 |
| `supabase/functions/send-push-notification/` | 알림 템플릿 확장 | 🟡 |

#### 알림 메시지 템플릿 확장
```typescript
// 스트릭 위기 알림
const streakWarningWithAd = {
  ko: {
    title: '⚠️ 스트릭 위험!',
    body: '오늘 체크하지 않으면 15일 스트릭이 끊어집니다. 광고를 보고 프리즈를 사용해보세요!',
  },
  en: {
    title: '⚠️ Streak at Risk!',
    body: 'Your 15-day streak will break if you don\'t check today. Watch an ad to freeze your streak!',
  }
}

// 복귀 유저 알림
const comebackWithBoost = {
  ko: {
    title: '👋 다시 오셨군요!',
    body: '광고를 보고 30분간 XP 2배 부스트를 받아보세요!',
  },
  en: {
    title: '👋 Welcome back!',
    body: 'Watch an ad to get 30 min XP 2x boost!',
  }
}
```

#### 변경 체크리스트
- [ ] 스트릭 위기 알림에 광고 CTA 추가
- [ ] 복귀 유저 알림에 XP 부스트 안내 추가
- [ ] 알림 클릭 시 앱 내 해당 기능으로 딥링크

---

### 11.6 구독 상태 관리

#### 영향받는 파일 (신규)
| 파일 | 역할 | 우선순위 |
|------|------|----------|
| `apps/mobile/src/hooks/useSubscription.ts` | 구독 상태 관리 훅 | 🔴 |
| `apps/mobile/src/contexts/SubscriptionContext.tsx` | 전역 구독 상태 | 🔴 |
| `apps/mobile/src/services/iapService.ts` | IAP 결제 처리 | 🔴 |
| `apps/web/src/hooks/useSubscription.ts` | Web 구독 상태 | 🟡 |

#### 전역 상태 설계
```typescript
interface SubscriptionState {
  // 구독 정보
  isPremium: boolean;
  planType: 'free' | 'monthly' | 'yearly' | 'lifetime';
  expiresAt: Date | null;

  // 광고 상태
  showAds: boolean;
  adFrequency: 'low' | 'medium' | 'high';

  // 기능 제한
  mandalartLimit: number; // 무료: 3, Premium: 무제한
  reportLimit: { weekly: number; diagnosis: number };

  // 보상 상태
  xpBoostAvailable: boolean;
  streakFreezeAvailable: boolean;
  yesterdayCheckAvailable: boolean;
}
```

#### 변경 체크리스트
- [ ] `user_subscriptions` 테이블 마이그레이션
- [ ] `useSubscription` 훅 구현 (Web, Mobile)
- [ ] 모든 화면에서 Premium 상태에 따른 분기 처리
- [ ] 만다라트 생성 시 개수 제한 체크

---

### 11.7 데이터베이스 마이그레이션 체크리스트

#### 필수 테이블
```sql
-- 1. 사용자 구독 (Phase 6)
CREATE TABLE user_subscriptions (...);

-- 2. 광고 시청 기록 (Phase 2)
CREATE TABLE ad_watch_history (...);

-- 3. 보상형 광고 쿨다운 (Phase 2)
CREATE TABLE rewarded_ad_cooldowns (...);

-- 4. XP 부스트 상태 (Phase 2)
CREATE TABLE xp_boosts (...);

-- 5. 리포트 사용 기록 (Phase 3)
CREATE TABLE report_usage (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users NOT NULL,
  report_type TEXT NOT NULL,
  access_method TEXT NOT NULL, -- 'free', 'rewarded_ad', 'premium'
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### RLS 정책
- [ ] 모든 신규 테이블에 user_id 기반 RLS 적용
- [ ] 구독 상태 조회 시 본인 데이터만 접근 가능

---

### 11.8 구현 순서 및 의존성

```
Phase 1: AdMob SDK 통합 + 배너 광고
    └→ 의존성: 없음
    └→ 영향: HomeScreen, TodayScreen, MandalartListScreen

Phase 2: 보상형 광고 (XP 부스트)
    └→ 의존성: xp_boosts 테이블, xpMultipliers.ts 수정
    └→ 영향: XP 시스템 전체, stats.ts

Phase 3: 리포트 생성 광고 연동
    └→ 의존성: report_usage 테이블, generate-report 수정
    └→ 영향: ReportsPage/Screen, generate-report Edge Function

Phase 4: 전면 광고 + Frequency Capping
    └→ 의존성: ad_watch_history 테이블
    └→ 영향: 만다라트 생성 완료 화면

Phase 5: 신규 유저 보호 기간
    └→ 의존성: user_created_at 확인 로직
    └→ 영향: 광고 표시 로직 전체

Phase 6: Premium 구독 (IAP)
    └→ 의존성: user_subscriptions 테이블, IAP SDK
    └→ 영향: 전체 앱 (기능 제한/해제)

Phase 7: Premium 기능 분기 처리
    └→ 의존성: useSubscription 훅
    └→ 영향: 모든 화면

Phase 8: 광고 피로도 추적/최적화
    └→ 의존성: ad_watch_history 분석
    └→ 영향: 광고 표시 로직
```

---

### 11.9 테스트 체크리스트

#### 단위 테스트
- [ ] XP 부스트 활성화/비활성화 로직
- [ ] 리포트 생성 권한 체크 로직
- [ ] 스트릭 프리즈 획득 로직
- [ ] 구독 상태 조회 로직

#### 통합 테스트
- [ ] 광고 시청 → XP 부스트 활성화 → XP 계산 시 2배 적용
- [ ] 광고 시청 → 리포트 생성 → 쿨다운 기록
- [ ] Premium 구독 → 광고 비표시 → 기능 제한 해제

#### E2E 테스트 (추후)
- [ ] 신규 유저: Day 1-3 광고 미노출 확인
- [ ] 무료 유저: 광고 노출 + 보상형 광고 작동
- [ ] Premium 유저: 광고 없음 + 모든 기능 사용 가능

---

## 12. 문서 간 연결

### 관련 문서
| 문서 | 연관성 |
|------|--------|
| [XP_SYSTEM_PHASE2_COMPLETE.md](./XP_SYSTEM_PHASE2_COMPLETE.md) | XP 부스트 확장 |
| [BADGE_SYSTEM_V5_RENEWAL.md](./BADGE_SYSTEM_V5_RENEWAL.md) | Premium 배지 추가 |
| [NOTIFICATION_SYSTEM_PROGRESS.md](./NOTIFICATION_SYSTEM_PROGRESS.md) | 광고 CTA 알림 |
| [ROADMAP.md](../project/ROADMAP.md) | Phase 10.2 진행 상태 |

### 코드 참조
| 파일 | 라인 | 수정 필요 내용 |
|------|------|---------------|
| `xpMultipliers.ts` | L13-20 | MultiplierType에 'ad_boost' 추가 |
| `stats.ts` | L5 | getActiveMultipliers 호출부 확장 |
| `badgeEvaluator.ts` | L22 | Premium 상태 체크 추가 |
| `generate-report/index.ts` | L13-17 | 접근 방식(free/ad/premium) 파라미터 추가 |

---

**문서 버전**: 1.1
**작성자**: Development Team
**최종 업데이트**: 2025-12-06
**다음 리뷰**: 각 Phase 시작 전
