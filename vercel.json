{
  "buildCommand": "pnpm --filter @mandaact/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --filter @mandaact/web...",
  "framework": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/web packages/shared"
}
